# Frontend environment variables
VITE_API_URL=http://localhost:3001/api

# false = all fleet features use the real backend (recommended for integration/demo)
# true  = use in-browser mock data (no backend required for UI-only work)
VITE_USE_MOCK=false
