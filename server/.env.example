# PostgreSQL connection string — set USER, PASSWORD, PORT for your install
# This machine often uses port 5433 for PostgreSQL 17
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5433/fleet_monitor

# Server port
PORT=3001

# Optional: comma-separated origins (default allows Vite localhost)
CORS_ORIGIN=http://localhost:5173
